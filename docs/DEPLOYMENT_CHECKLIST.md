# Deployment Checklist

Before deployment:

- Run the test suite and compile check.
- Verify the application starts with the intended configuration.
- Confirm routes return the expected health and workflow responses.
- Review monitoring, drift, latency, and freshness signals.

After deployment, exercise one representative workflow from ingestion through monitoring and record the release identifier with the result.